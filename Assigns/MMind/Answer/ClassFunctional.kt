
// We will use Pair<Int, Int> for 2-D points.  Tack a distance function onto
// that class.

fun main() {
   val rnd = // Make a Java Random
   val pts = // Array of four randomly set points.  use "to"

   val pairs = // List of all pairs of points, except self-pairs (two different map functions will be needed)
   // Dump all pairs.forEach({it.forEach({print("${it.first} <-> ${it.second}  ")}); println()});

